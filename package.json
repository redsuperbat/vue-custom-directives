{
  "name": "vue-custom-directives",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "deploy": "func() { npm run build && git add . && git commit -m \"$1\" && git push origin master; }; func"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/redsuperbat/vue-custom-directives.git"
  },
  "keywords": [],
  "author": "redsuperbat",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.13",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
